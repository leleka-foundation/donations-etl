Place your organization's logo here as `logo.png`.

Recommended: PNG format, at least 200px wide, transparent background.

The logo appears in the letterhead of donor confirmation letters.
Set the `LOGO_PATH` environment variable if using a different path.
