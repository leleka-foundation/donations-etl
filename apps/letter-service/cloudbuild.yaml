steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t',
        '$_IMAGE_URI',
        '-f',
        'apps/letter-service/Dockerfile',
        '.',
      ]
images:
  - '$_IMAGE_URI'
